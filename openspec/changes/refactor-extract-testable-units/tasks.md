## Notation

Each extraction is "component + parent route update + colocated test, in one commit." Tasks marked **(unblocks …)** cite which previously-SKIP'd scenarios from `backfill-test-citations` become reachable.

Component file paths follow the test-layout convention: tests are colocated as `<Name>.svelte.test.ts` next to the component, and route tests drop the `+` prefix (`page.test.ts`, not `+page.test.ts`).

## 1. Wizard — pure helpers (smallest extraction first)

- [x] 1.1 Extract `deriveStep4Title(input): string` into `src/lib/component/wizard/title.ts` (pure function reading `wizardInput`).
- [x] 1.2 Update `src/routes/(app)/wizard/+page.svelte` to import `deriveStep4Title` for `currentConfig.title` on step 4.
- [x] 1.3 Add `src/lib/component/wizard/title.test.ts` citing `[OB-014]` — hold vs. loss vs. gain title flip.
- [x] 1.4 Extract `performSetup(input, deps): Promise<SetupResult>` into `src/lib/component/wizard/setup-orchestration.ts`. Dependencies injected: `updateUser`, `updateBodyData`, `wizardCreateTargets`, plus optional `onStepStart(step)` for progress-message choreography (the route owns `goto` + fade-out FX per design Decision D2).
- [x] 1.5 Update `src/lib/component/wizard/Setup.svelte` to import `performSetup` and pass real Tauri commands as deps.
- [x] 1.6 Add `src/lib/component/wizard/setup-orchestration.test.ts` citing `[OB-015]` (happy path), `[OB-016]` (rollback when `wizardCreateTargets` throws), `[OB-017]` (all-three persisted), `[PF-015]` (new targets after wizard re-run).

## 2. Export — progress modal

- [x] 2.1 Extract `ExportProgressModal.svelte` into `src/lib/component/export/`. Props: `dialog` (bindable), `message`, `stage`, `progress`, `isExporting`, `filePath`, `bytesInfo`, callbacks `oncancel`, `onclose`. Close is `disabled={isExporting}` (preserved from original to honor Non-Goal "no interaction redesign"; design.md proposed `stage === 'complete'` but that would have been stricter than current shipping behavior).
- [x] 2.2 Update `src/routes/(app)/export/+page.svelte` to compose the new modal. Removed inlined `LoadingIndicator`/`ModalDialog` block + unused `stageIcons`/`stageIcon` derivation + phosphor icons that only lived inside the modal.
- [x] 2.3 Add `src/lib/component/export/ExportProgressModal.svelte.test.ts` — 9 tests citing `[EX-007]` (Close disabled/enabled), `[EX-006]` (Cancel enabled then disabled), `[EX-004]` (file-path alert), `[STG-001..STG-003]` (badge state transitions per progress %).

## 3. Profile — edit modal

- [x] 3.1 Extract `ProfileEditModal.svelte` into `src/lib/component/profile/`. Props: `dialog` (bindable), `entry` (bindable LibreUser | null/undefined), `errorMessage`, callbacks `onsave`, `oncancel`. Owns its `showAvatarPicker` toggle internally; resets the picker view each time the modal opens via `$effect` tracking entry-presence transitions.
- [x] 3.2 Update `src/routes/(app)/profile/+page.svelte` to compose the new modal; swipe-to-open `SwipeableListItem` stays in the route. Removed inline `ModalDialog`/`Avatar`/`ValidatedInput`/`AvatarPickerContent` imports + the route-local `showAvatarPicker` state + `handleCancel` (the modal handles its own reset).
- [x] 3.3 Add `src/lib/component/profile/ProfileEditModal.svelte.test.ts` — 7 tests citing `[PF-003]` (bind:entry round-trips name mutation), `[PF-008]..[PF-011]` (nickname bounds 2/40 enforced via native `minLength`/`maxLength`), `[MOD-001]` (errorMessage surfaces as alert), `[MOD-004]` (avatar click opens picker).

## 4. Weight — modal

- [x] 4.1 Extract `WeightModal.svelte` into `src/lib/component/weight/`. Props: `dialog` (bindable), `entry` (bindable), `errorMessage`, `min` (default 30), `max` (default 330), callbacks `onsave`, `oncancel`. `ondelete` deferred — the existing `modalWeight` config explicitly throws on delete (`'Delete not supported for weight entries'`); no use case in shipping. Uses the custom `footer` snippet pattern to dodge the veilchen `ModalDialog` close-before-await bug (issue #1 upstream).
- [x] 4.1a Tighten `[WT-009]`: clamp on commit (blur via bubbling `change` event + a `handleSave` wrapper as defense-in-depth) and disable Save while the in-progress value is out of bounds. Backend remains authoritative. Veilchen `NumberStepper` typed-input non-clamp filed upstream as issue #3; the local clamp can be removed once that lands.
- [x] 4.2 Update `src/routes/(app)/+page.svelte` (dashboard) to compose the new modal in place of the inlined weight flow. Removed the inline `<ModalDialog>` block (~35 lines) + dropped now-unused `NumberStepper` and `display_date_format` imports.
- [x] 4.3 Add `src/lib/component/weight/WeightModal.svelte.test.ts` — 9 tests citing the rewritten `[WT-009]` (typing leaves entry.amount unclamped + Save disabled while OOB; blur clamps below-min up and above-max down; `[VAL-007]` for the backend-error alert), `[MOD-001]` (error alert), `[MOD-004]` (dialog open/close via show/close), plus Save/Cancel callback wiring and the existing -/+ button clamp.
- [x] 4.4 Add `specs/weight-tracking/spec.md` delta to this change documenting the modified `Weight value bounds` requirement and rewritten `[WT-009]`. Update `proposal.md` _What Changes_ / _Modified Capabilities_ / non-goals accordingly. Note that `[VAL-008]` loses its only feature citation as a side effect — flagged in the proposal but not addressed here.
- [x] 4.5 Generalize the UX rules surfaced during weight-modal smoke testing into `_conv-validation`. Add `specs/_conv-validation/spec.md` delta with new requirement `Inline feedback for invalid input` containing `[VAL-012]` (validation message visible when input invalid) and `[VAL-013]` (submit disabled while any required field invalid). Cite the new IDs from `WeightModal` and `ProfileEditModal` tests.
- [x] 4.6 Extract the validity-tracking pattern into a reusable composable at `src/lib/composition/useFieldValidity.svelte.ts`. Sits one layer below `useEntryModal`: the modal composable owns lifecycle + backend round-trip; this one owns a single input's _displayed_ validity in response to live keystrokes. Caller passes a predicate `(rawValue) => boolean` and gets back `{ displayValid, handleInput, revalidate }`. WeightModal and ProfileEditModal now consume it; the upcoming `IntakeMask` will too. Tests: 7 new in `useFieldValidity.test.ts` cite `[VAL-012]` / `[VAL-013]` at the composable layer; modal-level tests stay green.
- [x] 4.7 Rewrite `[WT-009]` again after smoke-testing revealed the blur clamp was surprising UX. Spec now: validation message + disabled Save guard out-of-bounds input; the typed value is _preserved_ (no silent snap). Removed `clampAmount()` and the `handleSave` wrapper from `WeightModal`; Save now wires straight to `onsave`. NumberStepper's internal +/- clamp stays — only direct typing preserves the value verbatim.
- [x] 4.8 Render the validation message inline below the stepper inside `WeightModal` rather than via `NumberStepper`'s `validator-hint` children slot — the slot's hint span renders horizontally inside the `.join` row and is effectively invisible to the user. The plain `<p class="text-error text-sm mt-2">` below the stepper is what the spec scenarios `[VAL-012]` actually verify.

## 5. Dashboard — intake FAB + modal + plan-review

- [x] 5.1 Extract `IntakeFab.svelte` into `src/lib/component/intake/`. Props: callback `onclick`. Renders the floating action button.
- [x] 5.2 Extract `IntakeModal.svelte` into `src/lib/component/intake/`. Props: `dialog` (bindable), `entry` (bindable), `mode: 'create' | 'edit'`, `enableDelete`, `errorMessage`, callbacks `onsave`, `oncancel`, `onrequestdelete`, `ondelete`. Single component composed twice by the route (once per dialog ref from `useEntryModal`). Title flips by mode; footer flips Save↔Delete by `enableDelete`. Adopts the `errorMessage` alert convention from WeightModal/ProfileEditModal (previously missing in the intake flow — pre-existing gap closed during extraction).
- [x] 5.3 Extract `PlanReviewPanel.svelte` into `src/lib/component/dashboard/`. Props: `expanded` plus the data props feeding `IntakePlanCard` (`dailyRate`, `recommendation`, `targetCalories`, `maximumCalories`, `averageIntake`) and `EncouragementMessage` (`daysElapsed`, `daysLeft`, `goalReached`). **Deviated from the planned `ontoggle` callback**: the dashboard's `showPlan` boolean also drives a second `{#if}` block (start/target weight labels + today's weight callout inside the _progress bar_ section), and the "Review plan" toggle button is part of the progress-bar header layout. Pulling those into the panel would have restructured the progress bar; leaving them in the route keeps the panel a pure visualization of `expanded`. No `ontoggle` — the route owns the toggle UI and the state.
- [x] 5.4 Update `src/routes/(app)/+page.svelte` to compose `IntakeFab`, `IntakeModal`, `PlanReviewPanel`. The route owns the data (`load` in `+page.ts`) and the Tauri command wiring (the `useEntryModal` callbacks for create/update/delete intake and weight). Final shape: 340 LOC (was 403 pre-refactor); remaining inline markup is the dashboard header (date + avatar), the progress bar with its `{#if showPlan}`-gated "Block 1" (start/target weight labels + today callout — left in place because it's coupled with the progress-bar layout and shares `showPlan` with the panel), and the content area (`IntakeScore`, `IntakeStack`, `WeightScore`). Could be further sliced (e.g. `WeightProgressDetail` for Block 1, `DashboardHeader` for avatar/date) but that's beyond this refactor's "make the dashboard testable" goal.
- [x] 5.4a Migrate `src/routes/(app)/history/+page.svelte` from its three duplicated `ModalDialog` blocks to `IntakeModal` (`mode='create'` / `'edit'` / `'delete'`). Extended `IntakeModal` to accept `mode='delete'` for the standalone swipe-to-delete dialog (read-only full form via `<fieldset disabled>`, Delete/Cancel footer, no trash button — entered directly via `openDelete()`, not via the in-place edit→trash flip). Removed now-unused `ModalDialog`, `IntakeMask`, `convertDateStrToDisplayDateStr` imports. Wasn't in the original plan — caught while smoke-testing 5.2.
- [x] 5.4b Title fix + trash-toggle UX (smoke-test follow-on for 5.4a): (1) modal title follows the _view_ not the _mode_, so the in-place edit→trash flip relabels to "Delete Intake"; (2) trash button now acts as a toggle — clicking it again exits delete-confirm and returns to edit (was: only Cancel-the-whole-modal as recovery, which threw away the user's edit context). Added `oncanceldelete` to IntakeModal as the symmetric callback to `onrequestdelete`; added `useEntryModal.cancelDelete()` as the symmetric method to `requestDelete`. Trash button gets `aria-pressed` reflecting `isDeleteView`. (3) `IntakeMask` API split: `compact?: boolean` (default false) drives summary-card vs full-form layout — formerly bundled with `readonly`; `readonly?: boolean` now means "wrap the full form in `<fieldset disabled>` so the modal stays the same size on trash-toggle." `IntakeStack` migrated from `readonly` → `compact`. Tests updated. **Asymmetric per design decision:** history's edit modal does NOT mount the trash button (`onrequestdelete` not passed) since history's row already has swipe-to-delete via SwipeableListItem. Dashboard's IntakeStack has no row-level delete affordance (swipe flips cards, long-press opens edit), so the in-edit trash button stays the only delete path there. Whether to add a dashboard-level row-delete gesture is deferred to a separate change.
- [x] 5.2a IntakeModal validation: added `useFieldValidity` with whole-entry `safeParse` against `NewIntakeSchema` per `[VAL-014]`. Moved the previously-top `errorMessage` div to a bottom `AlertBox` showing the union of backend `errorMessage` and the schema-driven `validity.errorMessage`. Save button shakes once per "still invalid" period (`[VAL-013]`). AlertBox hidden in delete-confirm view. Same convergence applied to `WeightModal` and `ProfileEditModal` so all three modals look identical (separate commit; see VAL-014 commit).
- [x] 5.5 Add `src/lib/component/intake/IntakeFab.svelte.test.ts` citing `[IT-005]` (click invokes onclick → opens create modal — covered by composition assertion).
- [x] 5.6 Add `src/lib/component/intake/IntakeModal.svelte.test.ts` — 7 tests citing `[IT-010]`/`[MOD-004]` (cancel without saving), `[IT-012]` (save propagates bound entry mutation), `[IT-013]`/`[MOD-002]` (trash → enableDelete flip → ondelete), `[IT-014]`/`[MOD-003]` (cancel during delete-confirm), `[MOD-001]` (×2: prefill + errorMessage alert, matching WeightModal precedent).
- [x] 5.7 Add `src/lib/component/dashboard/PlanReviewPanel.svelte.test.ts` — 3 tests citing `[PR-001]` (initial-expanded and prop-flip-to-expanded both surface IntakePlanCard content) and `[PR-002]` (initial-collapsed renders nothing). **Did NOT multi-cite `[ANI-001..ANI-003]`** — those scenarios are about route changes / settings menu / history day swipe, none of which the plan-review panel exercises. The slide+fly transitions on this panel aren't covered by a current ANI scenario, and asserting them through jsdom is unreliable anyway (the OUT transition keeps elements mounted indefinitely without a working frame ticker).

## 6. History — day card first, then week pager

- [x] 6.1 Extract `HistoryDayCard.svelte` into `src/lib/component/history/`. Props: `intakeTarget`, `intakeEntries` (Intake[]), `weightEntries` (WeightTracker[]), callbacks `ondayswipe`, `oneditintake`, `ondeleteintake`, `onaddintake`, `oneditweight`, `oncreateweight`. Expanded prop set from the original plan to cover the weight section + day-swipe gesture (both live inside the card, not the parent). Card is the full inside-the-`{#key selectedDateStr}`-block content: IntakeScore (with day-swipe), category badge row, intake entry list with SwipeableListItem (long-press alt entry), empty state, "Add Intake" button, weight section. Reads `foodCategories` from context. Outer `{#key}` + fly transitions stay in the route.
- [x] 6.2 Update `src/routes/(app)/history/+page.svelte` to mount `HistoryDayCard` inside the `{#key selectedDateStr}` block (single day at a time — the route iterates dates only in the week-pager header). Dropped unused imports: `IntakeScore`, `SwipeableListItem`, `longpress`, `ForkKnife`, `HandTap`, `Pencil`, `Trash`, `getFoodCategoryIcon`, `getFoodCategoryLongvalue`. Route line count down from 492 → ~325. The day-swipe gesture now fires from inside the card; `handleDaySwipe` and the day-state updates stay in the route.
- [x] 6.3 Add `src/lib/component/history/HistoryDayCard.svelte.test.ts` — 7 tests citing `[HI-008]` (category badges highlight tracked categories), `[HI-009]` (Add Intake button → onaddintake), `[HI-010]`/`[HI-011]` (intake row renders per entry; structural verification of swipe wiring), `[HI-010]` (long-press alternative entry — structural check via row presence), `[HI-012]` (weight-present branch — no "Tap to update"), `[HI-013]` (no-weight branch — "No weight tracked" + Update affordance fires oncreateweight), plus empty-state. **Did NOT cite `[HI-005]`** — date selection lives in the route's week pager, not the day card; will land in `HistoryWeek.svelte.test.ts` (6.6). **Did NOT cite `[GES-*]`** — jsdom can't simulate the actual swipe gesture, and SwipeableListItem's internal pointer handling isn't observable through the component boundary; tests verify the wiring structurally (each row mounted, callbacks prop-passed) rather than asserting gesture-driven invocation. NumberFlow's value rendering not asserted — uses a custom element that jsdom doesn't execute.
- [x] 6.4 Extract `HistoryWeek.svelte` into `src/lib/component/history/`. Props: `dates` (string[]), `selectedDateStr`, `showRightCaret`, `caloriesAverage`, callbacks `onselect` (day pill click), `onweekchange` (`'previous' | 'next'` — fired by carets and by week-swipe gesture). Component encapsulates the header (month label + avg kcal) and the date selector grid, including the swipe gesture and the active-day styling logic (`getActiveClass`). Data fetching stays in the route — `scrollLeft` / `scrollRight` / `updateRange` invoke `getTrackerHistory` on `onweekchange`. Component derives the month label from `selectedDateStr`, so the route doesn't have to compute it.
- [x] 6.5 Update `src/routes/(app)/history/+page.svelte` to compose `HistoryWeek`. Route is now 326 LOC (was 492 pre-refactor). Removed: `getActiveClass`, `handleWeekSwipe`, `foodCategories` declaration, `getCategoriesContext` import (now read by the day card directly), `NumberFlow`, `CaretLeft`, `CaretRight`, `swipe` imports (all moved into HistoryWeek). The route now owns only data orchestration: `selectedDateStr`, `swipeDirection`, the two `useEntryModal` instances, `handleDaySwipe` (which can cross week boundaries), and the data-fetch helpers (`scrollLeft`/`scrollRight`/`updateRange`).
- [x] 6.6 Add `src/lib/component/history/HistoryWeek.svelte.test.ts` — 8 tests citing `[HI-001]` (current week renders one pill per date; selected pill carries active class), `[HI-002]` (previous-week and next-week caret clicks fire `onweekchange` with the right direction), `[HI-003]` (no future data → next-week caret affordance not rendered when `showRightCaret=false`), `[HI-004]` (month label + "avg kcal/day" label render), plus day-pill click → onselect with the date string. **Did NOT cite `[HI-006]`** — day-swipe gesture lives in `HistoryDayCard`, not `HistoryWeek`. **Did NOT cite `[HI-007]`** — swipe-across-week-boundary is a route-level behavior (`handleDaySwipe` decides whether to call `scrollRight()` when at end-of-week); not testable from `HistoryWeek` in isolation, and jsdom can't simulate swipe gestures anyway.

## 7. Cross-cutting test citations to backfill

- [x] 7.1 Audited `_conv-gestures`. **`[GES-001]`** + **`[GES-002]`** in new `src/lib/gesture/long-press.test.ts` (fake-timer unit test of the action). **`[GES-005]`** (swipe intake cards) in `IntakeStack.test.ts` — touch→swipe is veilchen's `Stack`; we verify the observable index→card mapping. **`[GES-006]`** (swipe weeks) in `HistoryWeek.svelte.test.ts` and **`[GES-007]`** (swipe days) in `HistoryDayCard.svelte.test.ts` — via dispatching the synthetic `swipe` CustomEvent the action emits. **`[GES-004]`** (swipe-right delete confirm) in `IntakeModal.svelte.test.ts` via the `mode="delete"` read-only-preview test. **`[GES-008]`** (swipe across week boundary) now cited in `history/page.test.ts` — render the page, dispatch a forward swipe at the last day, assert `getTrackerHistory` is called (the adjacent-week load). Routes ARE unit-testable here (existing pattern in `import/page.test.ts` etc.) — my initial "route-level orphan" call was wrong.
- [x] 7.2 Audited `_conv-animations`. All three now cited structurally (jsdom can't run the animation, so each test asserts the element carrying the transition renders, with a comment): **`[ANI-001]`** route-change fade in `layout.render.test.ts` (renders `+layout.svelte` with a `createRawSnippet` child + mocked `$app/state`/`$app/navigation`; asserts the fade-wrapped keyed content renders); **`[ANI-002]`** settings fly in `Settings.svelte.test.ts`; **`[ANI-003]`** history day-slide in `history/page.test.ts`. Earlier "acceptable orphan" call was wrong — the traceability gate flags any uncited ID regardless of tasks.md notes, so a real citation is required.
- [x] 7.3 **`[VAL-008]`** cited at the backend in `src-tauri/tests/cmd/test_weight_cmd.rs` (`weight_below_backend_lower_bound_rejected`, multi-cite with `[WT-007]`). Rationale corrected: `[VAL-014]` (schema-driven UI validation) only gatekeeps _structured, to-be-persisted_ entries — partial/custom payloads still reach the backend, which stays authoritative. The Rust test submitting an out-of-range value and getting rejected IS VAL-008's contract. (My earlier "obsolete" call was wrong.)
- [x] 7.4 Re-checked app-shell. **`[AS-002]`** in `Settings.svelte.test.ts` (floating-menu-overlay renders when open). **`[AS-008]`** in `useEntryModal.test.ts` (the success-callback hooks are the view-refresh mechanism). **`[AS-007]`** (page-nav refreshes data) now cited via load-function tests — `page.load.test.ts` (dashboard: `dailyDashboard` called + `depends('data:dashboardData')` registered) and `history/page.load.test.ts` (`getTrackerHistory`/`getLastIntakeTarget` called fresh). Testing the `load` fn directly is the cheapest correct layer — initial "route orphan" call was wrong.
- Net: section 7 cleared 13 orphans (32→19). Remaining 19 are all out-of-scope for this change: `[ERR-003..007]` (handed to `add-toast-emission-tests`, task 8.5), `[TRC-001..009]` (traceability meta-convention), `[AS-001]`/`[AS-009]` + `[VAL-003]`/`[VAL-009]`/`[VAL-010]` (app-shell/backend scenarios owned elsewhere).

## 8. Validate and ship

- [ ] 8.1 Run `npm run test:ci` — all Vitest tests green, including the new ones.
- [ ] 8.2 Run `cargo nextest run` — no Rust regression (this is a frontend-only refactor).
- [ ] 8.3 Run `npm run test:traceability` — confirm gate moves from 100/163 toward ≥150/163. Print the remaining orphans; they should be only `[ERR-003..ERR-007]` (handed off to `add-toast-emission-tests`) plus any TRC/VAL meta-orphans documented as acceptable in `backfill-test-citations` §11.
- [ ] 8.4 Manual smoke each affected route: dashboard (intake create/edit/delete, weight modal, plan-review toggle, pull-to-refresh), history (week navigation, day card swipes, add-intake-on-date), wizard (full 5-step happy path), profile (swipe-to-edit, save, cancel), export (run an export to completion, confirm Close enables).
- [ ] 8.5 If `backfill-test-citations` is still open, return there and tick the previously-SKIP'd rows that this change unblocked; otherwise note in the archive of this change which scenarios it newly covered.
- [ ] 8.6 Archive with `openspec archive --skip-specs refactor-extract-testable-units` (no spec deltas to apply).
