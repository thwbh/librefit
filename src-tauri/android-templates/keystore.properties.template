# Android Keystore Configuration
# This file is used to configure the signing keystore for Android builds
#
# For local development: Copy this to gen/android/keystore.properties and fill in your values
# For CI/CD: These values should be set via environment variables or secrets

storeFile=${KEYSTORE_FILE}
storePassword=${KEYSTORE_PASSWORD}
keyAlias=${KEY_ALIAS}
keyPassword=${KEY_PASSWORD}
