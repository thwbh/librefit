# Dependency Review Workflow
# Scans pull requests for vulnerable dependencies

name: 'Dependency Review'
on: [pull_request]

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Repository'
        uses: actions/checkout@v4
      
      - name: 'Dependency Review'
        uses: actions/dependency-review-action@v4
        with:
          # Fail on critical vulnerabilities
          fail-on-scopes: 'development,production'
          # Allow low-severity in development
          allow-licenses: 'MIT,Apache-2.0,BSD-3-Clause,ISC'
          # Comment on PR with findings
          comment-summary-in-pr: true

  npm-audit:
    name: 'npm Security Audit'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Repository'
        uses: actions/checkout@v4
      
      - name: 'Set up Node'
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
      
      - name: 'Install Dependencies'
        run: npm ci
      
      - name: 'Run npm audit'
        id: npm-audit
        run: |
          AUDIT_OUTPUT=$(npm audit --production 2>&1 || true)
          echo "audit_output<<EOF" >> $GITHUB_OUTPUT
          echo "$AUDIT_OUTPUT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        continue-on-error: true
      
      - name: 'Comment audit results'
        if: always() && steps.npm-audit.outputs.audit_output != ''
        uses: actions/github-script@v7
        with:
          script: |
            const auditOutput = `${{ steps.npm-audit.outputs.audit_output }}`;
            if (auditOutput.includes('vulnerabilities')) {
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `🚨 npm audit found vulnerabilities:\n\`\`\`\n${auditOutput}\n\`\`\``
              });
            }

  cargo-audit:
    name: 'Rust Security Audit'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Repository'
        uses: actions/checkout@v4
      
      - name: 'Install Rust'
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: clippy
      
      - name: 'Install cargo-audit'
        run: cargo install cargo-audit
      
      - name: 'Run cargo audit'
        working-directory: src-tauri
        run: cargo audit
        continue-on-error: true