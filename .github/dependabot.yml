# LibreFit Dependabot Configuration
# Automated dependency updates and security scanning.
#
# Note on auto-merge: Dependabot has no auto-merge setting in this file. To
# auto-merge (e.g. low-severity/patch updates), add a separate GitHub Actions
# workflow that runs `gh pr merge --auto` on `dependabot/*` PRs, plus enable
# "Allow auto-merge" + branch protection in repo settings.

version: 2
updates:
  # npm dependencies (JavaScript/TypeScript)
  - package-ecosystem: "npm"
    directory: "/"
    target-branch: "deps"
    schedule:
      interval: "daily"
      time: "09:00"
      timezone: "Europe/Vienna"
    open-pull-requests-limit: 10
    reviewers:
      - "thwbh"
    assignees:
      - "thwbh"
    labels:
      - "devops"
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"
    groups:
      # Tauri packages get their own PR so they land alongside the matching
      # cargo `tauri` group — the Rust crate and npm package for each plugin
      # (e.g. tauri-plugin-log / @tauri-apps/plugin-log) must share the same
      # major/minor or the Tauri build fails its version check.
      tauri:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        patterns:
          - "@tauri-apps/*"
      # Group non-major updates together
      npm-minor-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        exclude-patterns:
          - "@sveltejs/*"
          - "svelte"
          - "vite"
          - "@tauri-apps/*"
      # Group major framework updates together
      npm-major-frameworks:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        patterns:
          - "@sveltejs/*"
          - "svelte"
          - "vite"
      # Group security updates into their own PR set
      npm-security:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      # Vitest UI server vulnerability does not affect our CLI-only usage.
      - dependency-name: "vitest"
        versions: [">=3.0.0 <4.1.0"]
      # Cookie parsing in a transitive dep through SvelteKit; validation in place.
      - dependency-name: "cookie"
        versions: [">=0.0.0 <0.7.0"]

  # Rust dependencies (Cargo)
  - package-ecosystem: "cargo"
    directory: "/src-tauri"
    target-branch: "deps"
    schedule:
      interval: "daily"
      time: "09:30"
      timezone: "Europe/Vienna"
    open-pull-requests-limit: 10
    reviewers:
      - "thwbh"
    assignees:
      - "thwbh"
    labels:
      - "devops"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      # Mirror of the npm `tauri` group so the Rust and npm halves of each
      # plugin bump together and can be reviewed/merged as a pair. Listed first
      # so tauri crates land here, not in the catch-all cargo-minor-patch group.
      tauri:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        patterns:
          - "tauri"
          - "tauri-*"
      cargo-minor-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
      cargo-security:
        applies-to: security-updates
        patterns:
          - "*"

  # GitHub Actions workflows
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "deps"
    schedule:
      interval: "weekly"
      time: "10:00"
      timezone: "Europe/Vienna"
    open-pull-requests-limit: 10
    reviewers:
      - "thwbh"
    assignees:
      - "thwbh"
    labels:
      - "devops"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      github-actions-updates:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
